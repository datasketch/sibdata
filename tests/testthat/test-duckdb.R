test_that("Duckdb works", {

  # con <- DBI::dbConnect(duckdb::duckdb(), "inst/db/duckdb",
  #                       read_only=TRUE)
  # # duckdb <- "inst/db/duckdb"
  # # duckdb::duckdb_register(con, "sibdata", duckdb)
  # con
  #
  # DBI::dbListTables(con)
  #
  # dbExecute(con, "INSTALL postgres_scanner;")
  #
  # tbl(con, "region") |>
  #   select(slug, label)
  #
  # sibdata_region()


})
